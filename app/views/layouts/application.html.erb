<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Comman</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <!-- Le fav and touch icons -->
    <link href="/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
<%#*<link href='http://fonts.googleapis.com/css?family=Dosis' rel='stylesheet' type='text/css'>%>
  </head>
  <body id="comman" data-controller="<%= controller_safe_name %>" data-action="<%= action_safe_name %>">

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <%= link_to "Comman", root_path, :class => "brand" %>
          <div class="container nav-collapse">
            <ul class="nav">
              <li class="<%= "active" if is_business_active? %>"><%= link_to t('namespace.business'), business_root_path %></li>
              <li class="<%= "active" if is_factory_active? %>"><%= link_to t('namespace.factory'), factory_root_path %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">

          <% if content_for? :sidebar %>
            <div class="sidebar span2">
              <div class="sidebar-nav">
                <ul class="nav nav-list">
                  <%= yield :sidebar %>
                </ul>
              </div>
            </div>
          <% end %>

          <% if content_for? :secondary_navigation %>
            <div id="secondary_navigation" class="<%= content_for?(:sidebar) ? "span10 offset2" : "span12"%>">
              <ul class="nav nav-pills">
                <%= yield :secondary_navigation %>
              </ul>
            </div>
          <% end %>

          <div class="main <%= content_for?(:sidebar) ? "span10 offset2" : "span12"%> <%= "content-with-secondary-toolbar" if content_for?(:secondary_navigation) %>">
            <%= render :partial => 'common/flash', :locals => { :flash => flash } %>
            <%= yield %>
          </div>

        </div>
      </div>

      <footer>
        <p>&copy; Unibras <%= Date.today.year %></p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <% if content_for? :javascript %>
      <%= yield :javascript %>
    <% end %>

  </body>
</html>
