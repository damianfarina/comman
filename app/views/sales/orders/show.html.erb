<%= render(
  "common/headings/page",
  breadcrumbs: [
    { title: t("navigation.dashboard"), path: sales_root_path },
    { title: t("sales.orders.index.title"), path: sales_orders_path },
    { title: @order.name, path: sales_order_path(@order) },
  ],
  title: @order.name,
  actions: [
    ({ label: t("navigation.edit"), path: edit_sales_order_path(@order), method: :get } if @order.quote?),
  ],
) %>

<%= render "sales/orders/order", order: @order %>
