<%= render(
  "common/headings/page",
  breadcrumbs: [
    { title: t("navigation.dashboard"), path: sales_root_path },
    { title: t("sales.orders.index.title"), path: sales_orders_path },
    { title: @sales_order.name, path: sales_order_path(@sales_order) },
  ],
  title: @sales_order.name,
  actions: [
    ({ label: t("navigation.edit"), path: edit_sales_order_path(@sales_order), method: :get } if @sales_order.quote?),
  ],
) %>

<%= render "sales/orders/sales_order", sales_order: @sales_order %>
